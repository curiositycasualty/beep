dist: focal

language: go

go:
  - "1.13.x"

services:
  - docker

addons:
  apt:
    packages:
      - figlet

env:
  global:
    - TREE=birch
  jobs:
    - BIRD=hummingbird
    - BIRD=eagle
    - BIRD=bluejay

# install:

cache:
  apt: true

stages:
  - Tree Bird Tests

jobs:
  include:
    - name: "Tree Bird Tests (Pine)"
      env: TREE=pine

script: make BIRD=$BIRD test
